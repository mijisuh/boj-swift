func calc(_ a: Int, _ b: Int) {
    print(a + b)
    print(a - b)
    print(a * b)
    print(a / b)
    print(a % b)
}

let inputs = readLine()!.split(separator: " ").map { Int($0)! }
calc(inputs[0], inputs[1])


/*

함수 호출 시 전달 인자 레이블을 생략하려면 매개변수 이름 앞에 _ 추가
- 매개변수(Parameter): 함수 내부에서 사용하는 변수
- 전달인자(Argument): 함수 호출시 사용하는 실제 값

*/